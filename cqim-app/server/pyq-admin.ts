/**
 * server/pyq-admin.ts - 自定义外链页面管理 API（Prisma SQLite 版）
 * 已废弃：路由已整合到 admin.ts 的 /api/admin/custom-pages
 * 此文件保留为空路由，避免 index.ts 导入报错
 */
import { Router } from 'express';
const router = Router();
export default router;
